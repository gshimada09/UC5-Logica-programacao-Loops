const prompt = require("prompt-sync")();
let soma = 0
let escolha = Number(prompt("Digite o numero de limite que queira! "));

for(let contador = 1; contador <= escolha; contador ++){
	console.log (soma=soma + contador) 

}